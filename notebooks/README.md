# notebooks

DocScanner'ı Colab/Jupyter üzerinden denemek için `.ipynb` dosyaları buraya konur.
Tipik akış: bir belge fotoğrafını base64'e çevir → `/api` endpoint'ine `DocumentCrop`
isteği gönder → dönen görseli `ScanEffect`'e ver → sonucu görselleştir.
